# Устранение проблем с SSH подключением

## Проблема: Запрашивается пароль при деплое

Если при запуске `./auto_deploy.sh` запрашивается пароль, это означает, что SSH ключ не используется.

### Решение 1: Проверьте наличие SSH ключа

```bash
ls -la ~/.ssh/selectel_key
```

Если файл существует, проверьте права доступа:
```bash
chmod 600 ~/.ssh/selectel_key
```

### Решение 2: Проверьте подключение вручную

```bash
ssh -i ~/.ssh/selectel_key root@84.38.182.210 "echo 'Connection test'"
```

Если это работает без пароля, значит ключ настроен правильно.

### Решение 3: Используйте правильный IP

Убедитесь, что используете правильный IP адрес сервера. В скрипте по умолчанию используется `84.35.184.210`, но вы указали `84.38.182.210`.

### Решение 4: Если пароль все равно запрашивается

Если у вас есть пароль от сервера, вы можете:

1. **Ввести пароль** (но это неудобно для автоматизации)
2. **Настроить SSH ключ на сервере**:

```bash
# На локальной машине
ssh-copy-id -i ~/.ssh/selectel_key.pub root@84.38.182.210

# Или вручную скопировать публичный ключ
cat ~/.ssh/selectel_key.pub | ssh root@84.38.182.210 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"
```

### Решение 5: Используйте переменную окружения

Вы можете указать путь к ключу явно:

```bash
export DEPLOY_SSH_KEY="$HOME/.ssh/selectel_key"
./auto_deploy.sh root@84.38.182.210
```

## Проверка работы скрипта

После исправлений проверьте:

```bash
# Запустите скрипт с выводом отладочной информации
bash -x ./auto_deploy.sh root@84.38.182.210 2>&1 | grep -i "ssh\|key"
```

Вы должны увидеть строку:
```
🔑 Using SSH key: /home/timur/.ssh/selectel_key
```

## Альтернатива: Использование пароля

Если вы не можете настроить SSH ключ, вы можете модифицировать скрипт для использования `sshpass`:

```bash
# Установите sshpass
sudo apt-get install sshpass

# Используйте в скрипте
sshpass -p 'your_password' ssh root@84.38.182.210 "command"
```

⚠️ **Внимание**: Хранение паролей в скриптах небезопасно!

## Рекомендации

1. **Всегда используйте SSH ключи** вместо паролей
2. **Проверяйте права доступа** на ключ (должно быть 600)
3. **Используйте правильный IP** адрес сервера
4. **Проверяйте подключение** перед запуском деплоя

